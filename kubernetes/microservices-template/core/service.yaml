# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: microservice
  labels:
    app.kubernetes.io/name: microservice
spec:
  selector:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

